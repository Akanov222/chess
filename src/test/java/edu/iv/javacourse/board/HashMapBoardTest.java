package edu.iv.javacourse.board;

import edu.iv.javacourse.piece.Piece;
import edu.iv.javacourse.piece.Queen;
import edu.iv.javacourse.piece.Rook;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

class HashMapBoardTest {
/*
    private final Board board = new HashMapBoard();

    @Test
    void setAndGetPieceShouldBeCorrectly() {
        Coordinates coordinates = new Coordinates(File.A, 1);
        Piece piece = new Rook(Color.WHITE);
        board.setPiece(coordinates, piece);
        assertThat(board.getPiece(coordinates)).isEqualTo(piece);
        assertThat(board.isSquareEmpty(coordinates)).isFalse();
    }

    @Test
    void removePieceShouldBeCorrectly() {
        Coordinates coordinates = new Coordinates(File.C, 3);
        Piece piece = new Queen(Color.BLACK);
        board.setPiece(coordinates, piece);
        board.removePiece(coordinates);
        assertThat(board.getPiece(coordinates)).isNull();
        assertThat(board.isSquareEmpty(coordinates)).isTrue();
    }

    @Test
    void isSquareEmptyShouldBeCorrectly() {
        Coordinates coordinates = new Coordinates(File.B, 2);
        assertThat(board.getPiece(coordinates)).isNull();
        assertThat(board.isSquareEmpty(coordinates)).isTrue();
    }*/
}
